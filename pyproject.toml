[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-market"
version = "1.0.0"
description = "CLI Agentic Market — supermercados como infraestructura para agentes IA"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "rich>=13.0",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "pydantic>=2.0",
]

[project.scripts]
market = "market_cli:main"
market-server = "market_server:main"
wongcli = "wongcli:main"

[tool.setuptools]
py-modules = ["market_cli", "market_server", "market_mcp", "wongcli"]
