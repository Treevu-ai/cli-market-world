# Bump this file (commit to main) to run GTM preflight + Daily Briefing on Actions (ad-hoc, not the full chain).
# Daily prod ritual: morning-ops-chain.yml @ 08:00 PET (13:00 UTC).
# Do not use "Re-run jobs" on old failed runs — they use stale workflow YAML.
# Last triggered: 2026-06-12T16:10:00Z (verify PAT + GTM smoke steps 5-6)
