From dfda4d449fe54197b7eb39f076fbaba9aefce79f Mon Sep 17 00:00:00 2001
From: Cursor Agent <cursoragent@cursor.com>
Date: Fri, 26 Jun 2026 17:15:57 +0000
Subject: [PATCH] fix(security): require API key on intelligence-tier /v1 intel
 routes

Add Depends(_v1_auth) + _require_auth to price-risk, inflation-report,
procurement-signal, alerts, affordability, regulatory, and moat/confidence.
Bump version to 1.11.4.
---
 market_core/api_routes.py      | 14 ++++++++++++++
 market_core/market_stats.py    |  2 +-
 pyproject.toml                 |  2 +-
 tests/test_api_routes_wave1.py | 29 +++++++++++++++++++++--------
 tests/test_api_routes_wave3.py |  9 +++++++--
 5 files changed, 44 insertions(+), 12 deletions(-)

diff --git a/market_core/api_routes.py b/market_core/api_routes.py
index 5ab7fff..9bd9fb9 100644
--- a/market_core/api_routes.py
+++ b/market_core/api_routes.py
@@ -122,8 +122,10 @@ def intel_price_risk(
     line: str | None = Query(None, description="supermercados, farmacias, electro"),
     days: int = Query(7, ge=1, le=90),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Price Risk Intelligence — which categories are becoming volatile?"""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
@@ -146,8 +148,10 @@ def intel_inflation_report(
     line: str | None = Query(None, description="supermercados, farmacias, electro"),
     days: int = Query(30, ge=1, le=365),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Inflation Intelligence — where is price pressure increasing?"""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
@@ -169,8 +173,10 @@ def intel_procurement_signal(
     country: str | None = Query(None, description="PE, AR, MX, BR, CO, CL"),
     line: str | None = Query(None, description="supermercados, farmacias, electro"),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Procurement Intelligence — when should I buy? Returns buy_now/monitor/wait."""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
@@ -194,8 +200,10 @@ def intel_price_deal_alerts(
     threshold_pct: float = Query(5.0, ge=0.1, le=100.0),
     limit: int = Query(10, ge=1, le=50),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Price deal alerts — products at or above threshold_pct discount vs list price."""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
@@ -224,8 +232,10 @@ def intel_affordability(
     line: str = Query("supermercados"),
     days: int = Query(30, ge=1, le=365),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Affordability OS — cost-of-living composite from shelf + macro signals."""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
@@ -245,8 +255,10 @@ def intel_regulatory(
     days: int = Query(90, ge=1, le=365),
     category: str | None = Query(None, description="food, energy, fx, pharma, transport"),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Regulatory context events that may explain shelf price moves."""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
@@ -640,8 +652,10 @@ def moat_confidence_v1(
     store: str | None = Query(None),
     name: str | None = Query(None),
     enveloped: bool = Query(True),
+    username: str = Depends(_v1_auth),
 ):
     """Crowd-sourced confidence tier from receipt confirmations."""
+    _require_auth(username)
     db = get_db()
     try:
         with timing() as t:
diff --git a/market_core/market_stats.py b/market_core/market_stats.py
index d43431a..dbe2687 100644
--- a/market_core/market_stats.py
+++ b/market_core/market_stats.py
@@ -82,7 +82,7 @@ def _live_golden_linkage_pct(fallback: float = 0.0) -> float:
 
 GOLDEN_LINKAGE_PCT = _live_golden_linkage_pct()
 PYPI_PACKAGE_NAME = "cli-market-world"
-PACKAGE_VERSION = "1.11.0"
+PACKAGE_VERSION = "1.11.4"
 LICENSE = "MIT"
 PYPI_URL = f"https://pypi.org/project/{PYPI_PACKAGE_NAME}/"
 PEPY_PROJECT_URL = f"https://pepy.tech/projects/{PYPI_PACKAGE_NAME}"
diff --git a/pyproject.toml b/pyproject.toml
index 5d5117c..564e2af 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,7 +4,7 @@ build-backend = "setuptools.build_meta"
 
 [project]
 name = "cli-market-core"
-version = "1.11.3"
+version = "1.11.4"
 
 description = "Cost-of-Living OS for LatAm — intelligence layer for CLI Market. market_optimize_purchase: one-call basket compare, TCO, substitutes, action links. 32 MCP tools (default profile). Dependency of cli-market-world."
 requires-python = ">=3.10"
diff --git a/tests/test_api_routes_wave1.py b/tests/test_api_routes_wave1.py
index 6f00721..0f56236 100644
--- a/tests/test_api_routes_wave1.py
+++ b/tests/test_api_routes_wave1.py
@@ -8,6 +8,7 @@ from datetime import datetime, timezone, timedelta
 import pytest
 from fastapi.testclient import TestClient
 
+from market_core import api_routes
 from market_core.dev_app import app
 
 
@@ -16,6 +17,13 @@ def client(isolated_db):
     return TestClient(app)
 
 
+@pytest.fixture
+def authed_client(isolated_db):
+    api_routes._auth_fn = lambda _auth: "wave1_user"
+    yield TestClient(app)
+    api_routes._auth_fn = None
+
+
 def _seed(db_path_unused, isolated_db):
     from market_core import get_db
 
@@ -37,14 +45,19 @@ def _seed(db_path_unused, isolated_db):
         db.close()
 
 
+def test_intel_routes_require_auth(client):
+    r = client.get("/v1/intel/affordability?country=PE&days=30")
+    assert r.status_code == 401
+
+
 def test_health(client):
     r = client.get("/health")
     assert r.status_code == 200
     assert r.json()["status"] == "ok"
 
 
-def test_intel_affordability_envelope(client):
-    r = client.get("/v1/intel/affordability?country=PE&days=30")
+def test_intel_affordability_envelope(authed_client):
+    r = authed_client.get("/v1/intel/affordability?country=PE&days=30")
     assert r.status_code == 200
     body = r.json()
     assert "data" in body
@@ -54,15 +67,15 @@ def test_intel_affordability_envelope(client):
     assert "affordability_score" in body["data"]
 
 
-def test_intel_regulatory(client):
-    r = client.get("/v1/intel/regulatory?country=PE&days=365")
+def test_intel_regulatory(authed_client):
+    r = authed_client.get("/v1/intel/regulatory?country=PE&days=365")
     assert r.status_code == 200
     events = r.json()["data"]["events"]
     assert isinstance(events, list)
     assert len(events) >= 3
 
 
-def test_intel_price_deal_alerts(client, isolated_db):
+def test_intel_price_deal_alerts(authed_client, isolated_db):
     from market_core import get_db
     from datetime import datetime, timezone
 
@@ -78,7 +91,7 @@ def test_intel_price_deal_alerts(client, isolated_db):
         db.commit()
     finally:
         db.close()
-    r = client.get("/v1/intel/alerts?product=arroz&store=metro&threshold_pct=5&enveloped=false")
+    r = authed_client.get("/v1/intel/alerts?product=arroz&store=metro&threshold_pct=5&enveloped=false")
     assert r.status_code == 200
     data = r.json()
     assert data["total"] >= 1
@@ -103,7 +116,7 @@ def test_basket_tco(client, isolated_db):
     assert data["tco_total"] >= 9.0
 
 
-def test_existing_intel_price_risk(client, isolated_db):
+def test_existing_intel_price_risk(authed_client, isolated_db):
     from market_core import get_db
 
     db = get_db()
@@ -112,6 +125,6 @@ def test_existing_intel_price_risk(client, isolated_db):
         db.commit()
     finally:
         db.close()
-    r = client.get("/v1/intel/price-risk?country=PE")
+    r = authed_client.get("/v1/intel/price-risk?country=PE")
     assert r.status_code == 200
     assert "data" in r.json()
diff --git a/tests/test_api_routes_wave3.py b/tests/test_api_routes_wave3.py
index f1cde24..28a2ea8 100644
--- a/tests/test_api_routes_wave3.py
+++ b/tests/test_api_routes_wave3.py
@@ -59,7 +59,7 @@ def test_receipts_submit_and_get(client, isolated_db):
     assert r2.json()["data"]["id"] == rid
 
 
-def test_moat_confidence(client, isolated_db):
+def test_moat_confidence(authed_client, isolated_db):
     from market_core import get_db
 
     db = get_db()
@@ -68,11 +68,16 @@ def test_moat_confidence(client, isolated_db):
     finally:
         db.close()
 
-    r = client.get("/v1/moat/confidence?product_id=p1&store=wong")
+    r = authed_client.get("/v1/moat/confidence?product_id=p1&store=wong")
     assert r.status_code == 200
     assert "confidence_tier" in r.json()["data"]
 
 
+def test_moat_confidence_requires_auth(client):
+    r = client.get("/v1/moat/confidence?product_id=p1&store=wong")
+    assert r.status_code == 401
+
+
 def test_ecosystem_launches(client):
     r = client.get("/v1/ecosystem/launches?topic=food&limit=3")
     assert r.status_code == 200
-- 
2.43.0

