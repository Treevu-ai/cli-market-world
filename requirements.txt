# PyPI pin — update after each cli-market-core release
cli-market-core==1.12.3

# Semantic moat — Golden Record persistence (SQLite dev / Postgres prod).
cli-market-index[postgres] @ git+https://github.com/Treevu-ai/cli-market-index@66e6c7ecdda42353fb99ea42f3790d35f5b9802a

# Runtime dep the web tier imports directly (also pulled in transitively by the
# core, kept explicit so the server's Playwright fallback always resolves).
# Exact-pinned (not >=) so a compromised future release isn't auto-installed
# on the next build — bump deliberately, same as cli-market-core/index above.
playwright==1.60.0

# collect_prices.py imports httpx directly. Also pulled in transitively by
# cli-market-core, but a Docker build hit ModuleNotFoundError with only the
# transitive resolution (2026-07-10 cli-market-collector outage) -- kept
# explicit so this doesn't depend on resolver behavior again. Exact-pinned
# for the same supply-chain reason as playwright above.
httpx==0.28.1
twilio==9.4.1
openai>=1.0.0
